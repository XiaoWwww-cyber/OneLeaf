# OneLeaf RAG 架构演进任务清单

## 阶段1：核心基石 - 结构化精细文档切片 (Structural Chunking) & 源追踪

该阶段目标抛弃大文件一次性 Embedding，利用 Markdown 结构分割 + 兜底滑动窗口产出有物理来源的 Chunk，提升匹配度，消灭幻觉。

### 1. 数据库模型重构 ([vector_db.rs](file:///c:/code/demo/OneLeaf/src-tauri/src/ai/vector_db.rs))
- [ ] 备份 `document_vectors` 表结构（现有的大块 Embedding 将直接清空/作废重新生成）。
- [ ] 修改 `document_vectors` 方案，增加 `chunk_index(INTEGER)` 和 `chunk_content(TEXT)` 字段。一个 Document 将对应多个 Vector Chunks。
- [ ] 重写 `VectorDb::insert` 方法，支持插入单个 Chunk 文本、索引以及对应的向量。
- [ ] 更新 `VectorDb::search` 返回格式，携带提取到的精确 `chunk_content`。

### 2. 构建结构化切片引擎 ([ai/document_parser.rs](file:///c:/code/demo/OneLeaf/src-tauri/src/ai/document_parser.rs) 或新建 `ai/chunker.rs`)
- [ ] **一层：语义/结构分割**：实现基于 Markdown 标题树（`#`, `##`）或段落双换行符（`\n\n`）的粗切工具。
- [ ] **二层：滑动窗口硬分割**：针对上流切出的超过长度阈值（如 `800` 字符）的超长巨型段落，调用字数限制切割并保留 100 字符的 Overlap。
- [ ] 确保函数能够将全量文本转化为带有逻辑独立性的 `Vec<String>` 片段数组。

### 3. 多片段异步入库改造 ([ai/knowledge_base.rs](file:///c:/code/demo/OneLeaf/src-tauri/src/ai/knowledge_base.rs))
- [ ] 修改 [add_document](file:///c:/code/demo/OneLeaf/src-tauri/src/ai/knowledge_base.rs#205-308)：从原来的只 [embed](file:///c:/code/demo/OneLeaf/src-tauri/src/ai/knowledge_base.rs#473-490) 一次文本，变成先传入切片引擎获取 `chunks` 列表。
- [ ] 遍历入库：利用 `tokio::task::spawn_blocking` 或异步并发流为每个 Chunk 并发生成向量插入数据库。（为防阻塞，控制线程池大小）
- [ ] **Tantivy 对接**：确保全文检索引擎也将文件按 Chunk 的粒度打入索引。

### 4. 检索上下文重组与元数据注入 ([commands/ai.rs](file:///c:/code/demo/OneLeaf/src-tauri/src/commands/ai.rs))
- [ ] 读取底层 `kb.search` 返回的多个细粒度 `chunk_content`，不再是一整篇。
- [ ] **注入物理溯源防幻觉**：拼装 Context 时，根据检索到的所处文档的 metadata （需要联表或者内存查找），强制在每个片段前加入溯源抬头。
  - 例：`【参考片段 N】(来源文件: XXX.md | 分类: XXX)\n 内容: [Chunk 内容]`
- [ ] 应用之前做好的基于长度拦截策略（例如限制拼接后仍不能超过 8000 字符防爆缸）。

## 下一步计划
- [ ] 混合检索 (并发查询，利用 RRF 合并相似度)。
- [ ] 将 ONNX 推理转移至专用的阻塞线程池（解决假死）。
